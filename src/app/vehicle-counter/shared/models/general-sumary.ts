import { VehicleCounterSummaryList } from "./vehicle-counter-summaryList";

export interface GeneralSummary {
    totalCarsGeneral: number;
    totalAmountGeneral: number;
    vehicleCounterSummaryList: VehicleCounterSummaryList[];
  }